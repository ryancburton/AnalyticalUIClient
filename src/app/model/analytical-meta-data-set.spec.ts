import { AnalyticalMetaDataSet } from './analytical-meta-data-set';

describe('AnalyticalMetaDataSet', () => {
  it('should create an instance', () => {
    expect(new AnalyticalMetaDataSet()).toBeTruthy();
  });
});
