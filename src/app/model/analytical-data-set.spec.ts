import { AnalyticalDataSet } from './analytical-data-set';

describe('AnalyticalDataSet', () => {
  it('should create an instance', () => {
    expect(new AnalyticalDataSet()).toBeTruthy();
  });
});
